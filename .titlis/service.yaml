apiVersion: titlis.io/v1
kind: Service
metadata:
  name: titlis-ui
spec:
  product: titlis
  team: platform
  tier: tier-2
  description: Frontend SPA do app principal (React/Vite).
  workloads:
    - titlis-ui
